You don't actually need fastfetch, since you have `macchina`.

Macchina is an alternative written in Rust, and Sinix has a config for it.

run in your terminal:
```bash
macchina
```

And you have this cool box with system info.
